"""Capture knowledge from YouTube and websites into a unified Obsidian vault.

A sibling of ``substack_toolkit``: same shape (capture -> catalog cache ->
Obsidian vault with shared topic notes), but the sources are YouTube videos
(transcripts + metadata) and web articles (readable text). Both kinds are matched
against the SAME canonical topic vocabulary, so a video and an article about the
same topic collapse to one cross-linked topic note.
"""
